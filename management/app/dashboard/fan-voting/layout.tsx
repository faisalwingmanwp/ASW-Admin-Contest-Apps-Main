import { Button } from "@/components/ui/button";
import Link from "next/link";


export default function FanVotingLayout({children}: {children: React.ReactNode}) {
    return (
        <div className="">
            {children}
        </div>
    )
}